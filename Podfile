# Uncomment the next line to define a global platform for your project
platform :osx, '10.13.5'

target 'DesenWords' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DesenWords
  pod 'HotKey'
  pod 'Magnet'

end
